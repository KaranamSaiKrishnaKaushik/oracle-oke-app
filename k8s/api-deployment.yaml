apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
  namespace: oracle-oke-app
spec:
  revisionHistoryLimit: 1
  replicas: 1
  selector:
    matchLabels:
      app: backend-oke-api-service
  template:
    metadata:
      labels:
        app: backend-oke-api-service
    spec:
      containers:
        - name: backend-oke-api-service
          image: dokkaushik/backend-oke-api-service:b97cbb4e697d5cdbc0500f0a38867ba585e2d42b
          ports:
            - containerPort: 5000
          env:
            - name: ASPNETCORE_URLS
              value: http://+:5000
          #readinessProbe:
          #  httpGet:
          #    path: /health
          #    port: 5000
          #  initialDelaySeconds: 5
          #  periodSeconds: 10
          #livenessProbe:
          #  httpGet:
          #    path: /health
          #    port: 5000
          #  initialDelaySeconds: 10
          #  periodSeconds: 20
